<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Car Rental</title>
</head>
<body style="background-color: #f0dda1;">
  <%- include ('partials/navbar.ejs') %>  

    <div class="container">
        <br>
        <% if (product) { %> <!-- Check if product data exists -->
            <div>
              <p class="text-center"><img src="/images/<%= product.image %>" class="img-fluid" style="max-width: 50%;"></p>
              <br>
              <br>
              <p><b>Car Name:</b><br> <%= product.CarName %></p>
              <p><b>Car Availability:</b><br> <%= product.availability %></p>
              <p><b>Price:</b><br> $<%= product.Price.toFixed(2) %></p> 
            </div>
        <% } else { %>
            <p>No car found.</p>
        <% } %>
        <a href="/">Back</a>
    </div>
    
</body>
</html>
